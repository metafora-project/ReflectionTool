package de.kuei.metafora.reflectiontool.server.xml;

//TODO: implement

public class PlanningToolLink {

	// public static void focusPlanningToolCard(LandmarkBar bar) {
	// String nodeId = bar.getId();
	// String plan = bar.getPlanningToolMap();
	// String token = ReflectionPanel.token;
	// String user = ReflectionPanel.user;
	//
	// System.err.println("focusPlanningToolCard: " + nodeId + ", " + plan
	// + ", " + token + ", " + user);
	//
	// if (nodeId != null && plan != null && token != null && user != null) {
	// try {
	// CommonFormatCreator cfc = new CommonFormatCreator(
	// System.currentTimeMillis(), Classification.other,
	// "CENTER_NODE", true);
	//
	// cfc.addUser(user, token, Role.receiver);
	// cfc.addContentProperty("SENDING_TOOL", "REFLECTION_TOOL");
	// if (ReflectionPanel.groupId != null)
	// cfc.addContentProperty("GROUP_ID", ReflectionPanel.groupId);
	// if (ReflectionPanel.challengeId != null)
	// cfc.addContentProperty("CHALLENGE_ID",
	// ReflectionPanel.challengeId);
	// cfc.addContentProperty("PLANNINGTOOL_NODE", nodeId);
	// cfc.addContentProperty("PLAN", plan);
	//
	// String xml = cfc.getDocument();
	//
	// System.err.println("XML: "+xml);
	//
	// XMPPConnection.getInstance().sendToCommand(xml);
	//
	// } catch (XMLException e) {
	// e.printStackTrace();
	// }
	// }
	// }

}
